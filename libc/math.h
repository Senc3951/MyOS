#pragma once

#include <stdint.h>

/**
 * @brief Calculate base to the power of exp.
 * 
 * @param base Base.
 * @param exp Exponent.
 * @return base to the power of exp.
*/
uint64_t pow(int base, int exp);